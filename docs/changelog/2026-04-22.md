# 2026-04-22 배포

## 배포 요약
Workin 알바 관리 서비스 초기 버전 배포 — 모노레포 전체 구조, API/웹/모바일 기반 기능, Docker 환경, GitHub 동기화 완료

---

## 변경사항

### ✨ 새 기능 (Features)

**API (NestJS · port 3000)**
- Auth: 회원가입 / 로그인 / 토큰 갱신 / 내 정보 조회 (JWT, bcrypt)
- Stores: 매장 CRUD, 직원 목록, 초대 코드 발급·검증 (Redis TTL 24h)
- Schedules: 스케줄 CRUD (날짜 범위 쿼리)
- Attendance: 출근·퇴근 체크, 출퇴근 내역 조회 (중복 출근 방지)
- Payroll: 급여 요약 조회 (year/month 파라미터)
- 공통: Swagger(`/api/docs`), ValidationPipe, JwtAuthGuard, RolesGuard, `@CurrentUser` 데코레이터

**웹 대시보드 (Next.js · port 3001)**
- 로그인 페이지 (Zustand 인증 스토어 persist)
- 대시보드: 실시간 출근현황, 오늘 스케줄, 급여 요약
- 스케줄: 주간 그리드 (실제 API 연동)
- 알바생 목록: 재직/퇴직 필터
- 출근현황: 30초 자동 갱신
- 급여: 월 선택, summary 형태
- 매장 설정: 매장 수정/삭제

**모바일 앱 (Expo · port 8081)**
- 로그인 (expo-secure-store 토큰 저장)
- 홈/출퇴근 탭: 출퇴근 상태 동적 표시, 30초 갱신
- 출퇴근내역 탭, 스케줄 탭, 급여 탭

**공유 패키지**
- `@workin/types`: User, Store, Schedule, Attendance, Payroll 타입
- `@workin/utils`: `formatKRW`, `formatMinutes`, `calcPayroll`, `calcWeeklyAllowance`, `formatTime`, `getKSTHour`, `getKSTDateStr`

### 🐛 버그 수정 (Bug Fixes)
- KST/UTC 시간 표시 오류 수정: `getHours()` 대신 UTC+9 오프셋 방식(`getUTCHours()`) 사용
  - 영향 파일: `packages/utils/src/format.ts`, `apps/web/src/app/(dashboard)/schedules/page.tsx`, `apps/web/src/app/(dashboard)/dashboard/page.tsx`, `apps/web/src/app/(dashboard)/attendance/page.tsx`, `apps/web/src/components/domain/schedule-block.tsx`
- Prisma seed 타임존 오류 수정: `setHours()`(로컬 타임존 의존) → `Date.UTC()` 기반 헬퍼 함수로 전면 재작성

### 🔒 보안 (Security)
- `.claude/settings.local.json` git 추적 해제 (`git rm --cached`)
- `.gitignore`에 `.claude/settings.local.json` 추가 — 머신별 설정 파일 커밋 방지

### 📝 문서 (Docs)
- `CLAUDE.md`: 프로젝트 전체 가이드 (GitHub URL, 환경, 기술스택, 아키텍처, KST 주의사항, 개발 규칙) 작성
- `CHECKLIST.md`: 전체 구현 현황 체크리스트 작성 (인프라, API, 웹, 모바일, 테스트, 2차 기능, 우선순위)
- `docs/STARTUP.md`: 로컬 개발 환경 기동 가이드 작성 (Docker 기반)
- `docs/changelog/`: 일자별 배포 변경사항 관리 구조 신설

### 🏗️ 인프라 (Infra)
- Turborepo 모노레포 초기 구성 (apps/*, packages/*)
- Docker Compose: PostgreSQL 16-alpine(5432) + Redis 7-alpine(6379)
  - `POSTGRES_INITDB_ARGS: "--encoding=UTF8 --lc-collate=C --lc-ctype=C"` 추가
  - `version: '3.9'` 제거 (Compose v2 obsolete 경고 해소)
- Prisma 마이그레이션(`20260421_init`) 적용
- 시드 데이터: 5명(오너+매니저+알바3) · 53스케줄 · 53출퇴근
- GitHub 저장소 생성 및 최초 push: https://github.com/M0JOM0JI/workin

---

## 주요 파일 변경 목록

| 파일 | 변경 내용 |
|------|----------|
| `apps/api/prisma/seed.ts` | `Date.UTC()` 기반으로 전면 재작성 (타임존 독립적) |
| `packages/utils/src/format.ts` | `formatTime`, `getKSTHour`, `getKSTDateStr` 추가 |
| `packages/utils/src/index.ts` | 위 함수 export 추가 |
| `apps/web/src/app/(dashboard)/schedules/page.tsx` | KST 기반 그리드 필터 적용 |
| `apps/web/src/app/(dashboard)/dashboard/page.tsx` | `formatTime` 적용 |
| `apps/web/src/app/(dashboard)/attendance/page.tsx` | `formatTime` 적용 |
| `apps/web/src/components/domain/schedule-block.tsx` | `formatTime` 적용 |
| `docker-compose.yml` | C locale 옵션, version 필드 제거 |
| `.gitignore` | `.claude/settings.local.json` 추가 |
| `CLAUDE.md` | 전체 가이드 신규 작성 |
| `CHECKLIST.md` | 전체 체크리스트 신규 작성 |
| `docs/STARTUP.md` | 기동 가이드 신규 작성 |
| `docs/changelog/README.md` | Changelog 작성 규칙 신규 작성 |

---

## 미구현 (다음 배포 예정)

| 항목 | 우선순위 |
|------|----------|
| 회원가입 페이지 (웹 + 모바일) | 🔴 |
| 스케줄 추가/삭제 모달 | 🔴 |
| 알바생 초대 UI | 🔴 |
| 모바일 실기동 검증 | 🔴 |
| 직원 상세·시급 수정 | 🟡 |
| 출퇴근 수동 수정 | 🟡 |
| 급여 직원별 상세 | 🟡 |
| API 유닛/E2E 테스트 | 🟢 |
