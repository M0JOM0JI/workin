# 2026-04-27 (2차)

## ✨ STEP 1: DB 스키마 확장 (마이그레이션 `20260427012210_add_v1_features`)

- `StoreStaff` 필드 추가: `contractHoursPerMonth`, `insuranceType` (enum: NONE/FOUR_MAJOR/THREE_THREE), `memo`
- `Schedule` 필드 추가: `isConfirmed`, `isRecurring`, `repeatRule`
- `Attendance` 필드 추가: `isAutoClockOut`, `memo`
- `Store` 필드 추가: `payDay`, `autoClockOut*` (3가지 모드), `nightShift*`, `overtime*`
- 새 모델: `AttendanceRequest`, `ScheduleRequest`, `WorkPreference`, `Notification`, `Announcement`, `Payroll`
- 새 enum: `AutoClockOutMode`, `InsuranceType`, `RequestStatus`, `ScheduleRequestType`, `NotificationType`

## ✨ STEP 2: 권한/역할 관리 API

- `PATCH /stores/:id/staffs/:staffId` — 메모·4대보험 유형·월 계약 근무시간 수정 지원 추가
- `PATCH /stores/:id/staffs/:staffId/role` — OWNER 전용 STAFF↔MANAGER 승격/강등
- `POST /stores/:id/staffs/:staffId/rehire` — 퇴직자 재고용 (leftAt → null, joinedAt 갱신)
- `POST /stores/:id/invite` — `role` 파라미터로 STAFF/MANAGER 역할 분리 (MANAGER 코드는 OWNER만 발급 가능)

## ✨ STEP 2: 직원 관리 웹 UI 업데이트 (`/staffs`)

- 초대 모달: 역할 탭 (알바생/매니저) 선택 후 코드 발급 (OWNER만 매니저 탭 노출)
- 직원 상세 모달: 계약 정보 섹션 추가 (시급·계약시간·보험유형·메모 통합 수정)
- 역할 변경 섹션: OWNER 로그인 시 매니저 승격/알바생 강등 버튼 표시
- 재고용 버튼: 퇴직 직원 상세 모달에서 재고용 처리 가능
- `useStaffs` 훅 타입 확장: `contractHoursPerMonth`, `insuranceType`, `memo` 필드 추가

## ✨ STEP 3: AttendanceRequest API (출퇴근 수정 요청)

- `POST /stores/:id/attendance-requests` — STAFF는 PENDING, OWNER/MANAGER는 즉시 APPROVED + 출퇴근 반영
- `GET /stores/:id/attendance-requests` — status 필터 지원 (OWNER/MANAGER)
- `GET /me/attendance-requests` — 내 요청 목록 (알바생)
- `PATCH /stores/:id/attendance-requests/:rid/review` — 승인(출퇴근 즉시 반영)/거절

## ✨ STEP 3: 자동퇴근 Cron (`AutoClockoutModule`)

- `@nestjs/schedule` 설치, `ScheduleModule.forRoot()` 등록
- `AutoClockoutService` — 5분마다 실행, 매장별 `autoClockOut` 설정 기준으로 미퇴근 자동 처리
- 3가지 모드: MIDNIGHT(당일 KST 자정), MAX_HOURS(출근 후 최대 N시간), SCHEDULE(스케줄 종료 후 N분)
- `UpdateStoreDto` 확장 — `autoClockOut*`, `nightShift*`, `overtime*`, `payDay` 필드 추가

## ✨ STEP 3: 웹 출퇴근 관리 `/attendance` 리뉴얼

- 탭 구조: '오늘 현황' / '수정 요청(N)' 탭 분리
- 수정 요청 탭: PENDING 요청 목록 + 처리 모달 (승인/거절, 메모 입력)
- 수동 등록 버튼: 오너/매니저가 특정 직원의 출퇴근을 직접 등록 (즉시 반영)
- 자동퇴근 배지: `isAutoClockOut` 레코드에 회색 '자동퇴근' 배지 표시

## ✨ STEP 3: 모바일 출퇴근 화면 기능 추가

- '누락 신청' 버튼: 헤더에서 새 출퇴근 신청 (attendanceId=null)
- 기록별 '수정 요청' 버튼: 바텀시트 모달에서 요청 시간·사유 입력 후 제출
- 검토 중인 요청 배너: PENDING 상태 요청을 상단에 별도 표시
- 자동퇴근 배지, 수정 요청 중 배너 표시

## ✨ STEP 4: 스케줄 확정 + 주간 복사 API

- `PATCH /stores/:id/schedules/:sid/confirm` — 확정 토글 (`isConfirmed` true↔false)
- `POST /stores/:id/schedules/copy-week` — 해당 주 시프트 전체를 +7일로 복사, 복사된 시프트는 미확정 상태

## ✨ STEP 4: 웹 스케줄 페이지 리뉴얼

- 블록 클릭 → 상세 모달: 날짜·시간·확정여부 표시, 시간 수정/확정 토글/삭제 버튼
- 확정된 블록: 진한 색상 + ✓ 뱃지로 시각 구분
- 헤더 '주간 복사' 버튼: 현재 주 → 다음 주 복사 후 자동으로 다음 주 이동
- 그리드 상단에 `N/M 확정` 카운터 표시

## ✨ STEP 5: 급여 고도화 API (`PayrollService` 전면 재작성)

- **주휴수당 자동 계산**: ISO week(월~일) 기준 15h 이상 근무 시 `(weekHours/40)×8×시급` 자동 산정
- **야간수당 계산**: KST 22:00~06:00(UTC 13:00~21:00) 구간 overlap 정밀 계산, 매장 `nightShiftMultiplier` 배율 적용
- **초과근무 수당 계산**: 스케줄 종료 시간 대비 초과 근무분 집계, `overtimeMultiplier` 배율 적용
- **4대보험 공제 분기**: `insuranceType` — NONE(0) / THREE_THREE(3.3%) / FOUR_MAJOR(9.4%) 자동 분기
- **급여 확정 API** `POST /stores/:id/payroll/:staffId/confirm` — Payroll 레코드 upsert, `isConfirmed=true`, `confirmedAt` 저장
- `@workin/utils` `calcDeduction` 함수 및 `InsuranceType` 타입 추가

## ✨ STEP 5: 웹 급여 페이지 전면 개편 (`/payroll`)

- 직원 행 클릭 → 상세 모달: 시급·총근무시간·기본급·주휴수당·야간수당·초과수당·공제·실지급액 breakdown 표시
- 확정 상태 배지: 미확정(노랑) / 확정(초록 + 확정일)
- 급여 확정 버튼: OWNER/MANAGER만 노출, 클릭 시 `POST .../confirm` 호출
- 직원 목록 헤더에 추가수당 열(+야간+초과+주휴) 표시, 보험 유형 서브텍스트
- `usePayroll` 훅 타입 전면 확장 + `useConfirmPayroll` mutation 추가

## ✨ STEP 8: Statistics 모듈

- **`GET /stores/:storeId/statistics/staffs`** — 직원별 월간 통계: 근무일수, 총 근무시간, 평균 근무시간/일, 지각 횟수(스케줄 대비 5분 초과), 결근 횟수(스케줄 있었으나 미출근)
- **`GET /statistics/summary`** — 오너 전용: 보유 매장 전체 출근 현황·총 근무시간 집계
- **웹 `/statistics` 페이지** 신규: 월 선택 네비게이션, 요약 카드(직원수/총근무/지각/결근), 직원별 테이블 (근무일·총시간·평균·지각·결근)
- 사이드바에 '근무 통계 📊' 메뉴 추가

## ✨ STEP 7: PATCH /auth/me API + 모바일 급여 breakdown + 프로필 탭

- **`PATCH /auth/me`** — 이름·전화번호 수정 API 추가 (AuthService + AuthController)
- **모바일 급여 탭 개편** (`(tabs)/payroll`): 기본급·주휴수당·야간수당·초과수당·공제·실지급 전체 breakdown, 확정 상태 배지 (미확정 노랑 / 확정 초록+날짜)
- **모바일 프로필 탭** (`(tabs)/profile`) 신규: 이메일 표시(수정 불가), 이름·전화번호 수정, 로그아웃 버튼 (Alert confirm)
- `(tabs)/_layout.tsx`에 프로필 탭 추가

## ✨ STEP 6: 웹 설정 페이지 (`/settings`) 확장

- **자동 퇴근 설정 섹션**: 활성화 토글 + 모드 선택(MIDNIGHT / MAX_HOURS / SCHEDULE) + 모드별 기준값 입력 (버퍼 분, 최대 시간) → `PATCH /stores/:id` 연동
- **수당 설정 섹션**: 야간수당 활성화 토글 + 배율 입력, 초과근무수당 활성화 토글 + 배율 입력
- **급여일 설정**: payDay(1~31) 입력, 저장 후 배지 표시
- **매니저 읽기 전용 처리**: MANAGER 로그인 시 노란 안내 배너 표시, 모든 입력 비활성화, 저장 버튼 숨김, 위험 구역 카드 숨김
- `Empty` 컴포넌트 `label` prop alias 추가 (기존 `title`의 호환 별칭)
