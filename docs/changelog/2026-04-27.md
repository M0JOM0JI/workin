# 2026-04-27 (2차)

## ✨ STEP 1: DB 스키마 확장 (마이그레이션 `20260427012210_add_v1_features`)

- `StoreStaff` 필드 추가: `contractHoursPerMonth`, `insuranceType` (enum: NONE/FOUR_MAJOR/THREE_THREE), `memo`
- `Schedule` 필드 추가: `isConfirmed`, `isRecurring`, `repeatRule`
- `Attendance` 필드 추가: `isAutoClockOut`, `memo`
- `Store` 필드 추가: `payDay`, `autoClockOut*` (3가지 모드), `nightShift*`, `overtime*`
- 새 모델: `AttendanceRequest`, `ScheduleRequest`, `WorkPreference`, `Notification`, `Announcement`, `Payroll`
- 새 enum: `AutoClockOutMode`, `InsuranceType`, `RequestStatus`, `ScheduleRequestType`, `NotificationType`

## ✨ STEP 2: 권한/역할 관리 API

- `PATCH /stores/:id/staffs/:staffId` — 메모·4대보험 유형·월 계약 근무시간 수정 지원 추가
- `PATCH /stores/:id/staffs/:staffId/role` — OWNER 전용 STAFF↔MANAGER 승격/강등
- `POST /stores/:id/staffs/:staffId/rehire` — 퇴직자 재고용 (leftAt → null, joinedAt 갱신)
- `POST /stores/:id/invite` — `role` 파라미터로 STAFF/MANAGER 역할 분리 (MANAGER 코드는 OWNER만 발급 가능)

## ✨ STEP 2: 직원 관리 웹 UI 업데이트 (`/staffs`)

- 초대 모달: 역할 탭 (알바생/매니저) 선택 후 코드 발급 (OWNER만 매니저 탭 노출)
- 직원 상세 모달: 계약 정보 섹션 추가 (시급·계약시간·보험유형·메모 통합 수정)
- 역할 변경 섹션: OWNER 로그인 시 매니저 승격/알바생 강등 버튼 표시
- 재고용 버튼: 퇴직 직원 상세 모달에서 재고용 처리 가능
- `useStaffs` 훅 타입 확장: `contractHoursPerMonth`, `insuranceType`, `memo` 필드 추가

## ✨ STEP 3: AttendanceRequest API (출퇴근 수정 요청)

- `POST /stores/:id/attendance-requests` — STAFF는 PENDING, OWNER/MANAGER는 즉시 APPROVED + 출퇴근 반영
- `GET /stores/:id/attendance-requests` — status 필터 지원 (OWNER/MANAGER)
- `GET /me/attendance-requests` — 내 요청 목록 (알바생)
- `PATCH /stores/:id/attendance-requests/:rid/review` — 승인(출퇴근 즉시 반영)/거절

## ✨ STEP 3: 자동퇴근 Cron (`AutoClockoutModule`)

- `@nestjs/schedule` 설치, `ScheduleModule.forRoot()` 등록
- `AutoClockoutService` — 5분마다 실행, 매장별 `autoClockOut` 설정 기준으로 미퇴근 자동 처리
- 3가지 모드: MIDNIGHT(당일 KST 자정), MAX_HOURS(출근 후 최대 N시간), SCHEDULE(스케줄 종료 후 N분)
- `UpdateStoreDto` 확장 — `autoClockOut*`, `nightShift*`, `overtime*`, `payDay` 필드 추가

## ✨ STEP 3: 웹 출퇴근 관리 `/attendance` 리뉴얼

- 탭 구조: '오늘 현황' / '수정 요청(N)' 탭 분리
- 수정 요청 탭: PENDING 요청 목록 + 처리 모달 (승인/거절, 메모 입력)
- 수동 등록 버튼: 오너/매니저가 특정 직원의 출퇴근을 직접 등록 (즉시 반영)
- 자동퇴근 배지: `isAutoClockOut` 레코드에 회색 '자동퇴근' 배지 표시

## ✨ STEP 3: 모바일 출퇴근 화면 기능 추가

- '누락 신청' 버튼: 헤더에서 새 출퇴근 신청 (attendanceId=null)
- 기록별 '수정 요청' 버튼: 바텀시트 모달에서 요청 시간·사유 입력 후 제출
- 검토 중인 요청 배너: PENDING 상태 요청을 상단에 별도 표시
- 자동퇴근 배지, 수정 요청 중 배너 표시
